# Парсер Telegram каналов для венчурных проектов

Этот инструмент помогает автоматически находить информацию о венчурных проектах и инвесторах в Telegram каналах.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Получение API ключей Telegram

1. Перейдите на https://my.telegram.org
2. Войдите используя ваш номер телефона
3. Перейдите в раздел "API development tools"
4. Создайте новое приложение (если еще не создавали)
5. Скопируйте **API ID** и **API Hash**

### 3. Настройка конфигурации

1. Скопируйте файл `config.json.example` в `config.json`:
   ```bash
   copy config.json.example config.json
   ```

2. Откройте `config.json` и заполните:
   - `api_id` - ваш API ID из my.telegram.org
   - `api_hash` - ваш API Hash
   - `phone` - ваш номер телефона в формате +7XXXXXXXXXX
   - `channels` - список каналов для парсинга (например: `["@vc_channel", "@startup_news"]`)
   - `limit` - количество последних сообщений для анализа (по умолчанию 100)

### 4. Запуск парсера

```bash
python telegram_parser.py
```

При первом запуске вам нужно будет:
1. Ввести код подтверждения, который придет в Telegram
2. Ввести пароль двухфакторной аутентификации (если включена)

Сессия сохранится в файле `vc_parser_session.session`, при следующих запусках авторизация не потребуется.

## 📊 Результаты

Парсер сохраняет результаты в двух форматах:
- **vc_projects.xlsx** - Excel файл с данными
- **vc_projects.csv** - CSV файл с данными

### Что извлекается:

- **Дата сообщения**
- **Канал** - название канала
- **Тип** - Проект / Инвестор / Другое
- **Название проекта** - автоматически извлекается из текста
- **Сумма инвестиций** - если упоминается в сообщении
- **Стадия раунда** - Pre-seed, Seed, Series A/B/C, Angel
- **Инвесторы** - имена инвесторов/фондов
- **Ссылки** - все URL из сообщения
- **Контакты** - email и Telegram контакты
- **Описание** - краткое описание (первые 500 символов)
- **Полный текст** - полный текст сообщения

## 🔍 Как найти каналы для парсинга

1. **Публичные каналы** - можно парсить по username (например: `@vc_channel`)
2. **Приватные каналы** - нужно быть подписанным, затем использовать username или ID

### Примеры популярных каналов (укажите свои):

- `@startup_news` - новости стартапов
- `@vc_insights` - венчурные инсайты
- `@angel_investors` - ангельские инвесторы

## ⚙️ Настройка поиска

Вы можете настроить ключевые слова для поиска в файле `telegram_parser.py`:

```python
# Ключевые слова для проектов
self.project_keywords = [
    'стартап', 'startup', 'проект', 'раунд', ...
]

# Ключевые слова для инвесторов
self.investor_keywords = [
    'инвестор', 'investor', 'фонд', 'fund', ...
]
```

## 📝 Пример использования

```python
from telegram_parser import TelegramVCParser
import asyncio

async def parse():
    parser = TelegramVCParser(
        api_id=12345678,
        api_hash="ваш_hash",
        phone="+7XXXXXXXXXX"
    )
    
    await parser.connect()
    
    # Парсинг одного канала
    results = await parser.parse_channel("@vc_channel", limit=50)
    
    # Сохранение результатов
    parser.save_to_excel(results, "my_results.xlsx")
    
    await parser.client.disconnect()

asyncio.run(parse())
```

## ⚠️ Важные замечания

1. **Соблюдайте лимиты Telegram API** - не делайте слишком много запросов за короткое время
2. **Проверяйте права доступа** - убедитесь, что у вас есть доступ к каналам
3. **Храните конфиденциальность** - не публикуйте файл `config.json` и `*.session` в публичных репозиториях
4. **Соблюдайте правила каналов** - некоторые каналы могут запрещать автоматический парсинг

## 🐛 Решение проблем

### Ошибка авторизации
- Проверьте правильность API ID и API Hash
- Убедитесь, что номер телефона указан в правильном формате (+7XXXXXXXXXX)
- Удалите файл `vc_parser_session.session` и попробуйте снова

### Канал не найден
- Проверьте правильность username канала
- Убедитесь, что вы подписаны на приватный канал
- Попробуйте использовать полную ссылку на канал

### Нет результатов
- Увеличьте значение `limit` в config.json
- Проверьте, что в канале есть сообщения с ключевыми словами
- Настройте ключевые слова в коде под ваши нужды

## 📞 Поддержка

Если возникли вопросы или проблемы, проверьте:
1. Правильность настройки config.json
2. Установлены ли все зависимости (`pip install -r requirements.txt`)
3. Есть ли доступ к интернету и Telegram API



